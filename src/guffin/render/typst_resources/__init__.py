"""Typst PDF resources — bundled package data for ``guffin``.

Holds the Bergfink Pandoc/Typst template (``bergfink.typst`` and its ``*.typ`` partials, including
the ``user_cfg.typ`` customization point) together with the ``typst_*.lua`` Pandoc filters applied
when converting to Typst.
"""
