"""Shared callout icons — bundled package data for ``guffin``.

A small set of SVG badge icons, one per gentle-clues callout function (``info``, ``memo``,
``example``, ``conclusion``, ``question``, ``tip``, ``success``, ``warning``, ``danger``,
``error``).  The same files drive the callout type indicator in both the PDF output (inlined into
the gentle-clues ``icon:`` argument) and the EPUB output (inlined into the callout label), so the
two formats render an identical icon set.
"""
