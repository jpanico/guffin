"""Transcribe: source-to-model transcription and text normalization.

Bridges a source store's :class:`~guffin.roam.node_tree.NodeTree` into the normalized
:mod:`guffin.model` representation (the source→model transform).  The reverse, model→output, lives
in :mod:`guffin.render`.
"""
